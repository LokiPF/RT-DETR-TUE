## Deterministic Training

Run ```./deterministic_training.sh```. The default seed is ```42```.


## Build TU Prototypes

Run ```./build_tu_prototype.sh```.

The Fréchet mean for the bboxes is saved in ```pretrained_weights/bbox_prototypes.pth```.